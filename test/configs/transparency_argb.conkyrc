# ARGB Transparency Example
# Demonstrates true transparency with compositor support
# Requires: running compositor (picom, compton, mutter, etc.)

# Window settings for ARGB transparency
own_window yes
own_window_type desktop
own_window_transparent yes
own_window_argb_visual yes
own_window_argb_value 180

# Window hints for desktop integration
own_window_hints undecorated,below,sticky,skip_taskbar,skip_pager

# Position and size
alignment top_right
gap_x 20
gap_y 40
minimum_width 280
minimum_height 150

# Display settings
background yes
font DejaVu Sans Mono:size=10
update_interval 1.0
double_buffer yes

# Colors
default_color ffffff
color0 88ccff
color1 aaaaaa

TEXT
${color0}System Monitor$color
${hr 1}
${color1}CPU:$color ${cpu}% ${cpubar 6}
${color1}RAM:$color ${mem}/${memmax}
${color1}Uptime:$color ${uptime_short}
