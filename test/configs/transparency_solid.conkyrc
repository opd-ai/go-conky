# Semi-Transparent Solid Background Example
# Demonstrates solid color with alpha transparency
# Requires: running compositor for transparency effect

# Window settings for semi-transparent solid background
own_window yes
own_window_type desktop
own_window_transparent no
own_window_argb_visual yes
own_window_argb_value 200
own_window_colour 1a1a2e

# Window hints for desktop integration
own_window_hints undecorated,below,sticky,skip_taskbar,skip_pager

# Position and size
alignment bottom_left
gap_x 20
gap_y 40
minimum_width 300
minimum_height 180

# Display settings
background yes
font DejaVu Sans Mono:size=10
update_interval 1.0
double_buffer yes

# Colors - bright text on dark background
default_color e0e0e0
color0 00ff88
color1 ff8800

TEXT
${color0}Solid Background Demo$color
${hr 1}
CPU: ${cpu}% ${cpubar 6}
RAM: ${memperc}% ${membar 6}
Swap: ${swapperc}% ${swapbar 6}
${color1}Network:$color ${downspeed eth0} / ${upspeed eth0}
