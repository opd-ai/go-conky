# Advanced legacy .conkyrc configuration for testing
# Tests multiple features and edge cases

# Display settings
background yes
font DejaVu Sans Mono:size=12
update_interval 2.5
double_buffer yes

# Window settings
own_window yes
own_window_type desktop
own_window_transparent no
own_window_hints undecorated,below,sticky,skip_taskbar,skip_pager
alignment bottom_right
minimum_width 400
minimum_height 300
gap_x 25
gap_y 50

# Color settings
default_color ff5500
color0 ffffff
color1 888888
color2 00ff00
color3 0000ff

TEXT
${color0}Advanced System Monitor$color
${hr 2}

${color1}CPU Information:$color
Core 0: ${cpu cpu0}% ${cpubar cpu0 6}
Core 1: ${cpu cpu1}% ${cpubar cpu1 6}
Frequency: ${freq_g} GHz
Load: ${loadavg}

${color1}Memory Usage:$color
RAM:  ${mem}/${memmax} (${memperc}%)
Swap: ${swap}/${swapmax} (${swapperc}%)
${membar 6}

${color1}Disk Usage:$color
Root: ${fs_used /}/${fs_size /} (${fs_used_perc /}%)
${fs_bar 6 /}
Home: ${fs_used /home}/${fs_size /home}
${fs_bar 6 /home}

${color1}Network (eth0):$color
Down: ${downspeed eth0} | Total: ${totaldown eth0}
Up:   ${upspeed eth0} | Total: ${totalup eth0}

${color1}Processes:$color
Running: ${running_processes}/${processes}
${color1}Top CPU:$color
${top name 1} ${top cpu 1}%
${top name 2} ${top cpu 2}%
${top name 3} ${top cpu 3}%

${color1}System:$color
Uptime: ${uptime}
Kernel: ${kernel}
